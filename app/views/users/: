<h1><%= link_to @user.name, profile_user_path(@user) %></h1>
<% if @user.picture.url %>
  <%= image_tag @user.picture.url %>
<% else %>
  <%= image_tag "no-picture.png" %>
<% end %>

<%= render 'relationships/follow_button', user: @user %>
<p>マイマップ一覧</p>
<%= link_to "マイマップから検索する", controller: "mymaps", action: "search", user_id: @user.id %>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Comment</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @mymaps.each do |mymap| %>
      <tr>
        <td><%= mymap.name %></td>
        <td><%= mymap.comment %></td>
        <td><%= link_to "詳細", mymap_path(mymap) %></td>
        <td><%= render 'user_mymaps/favorite_button', mymap: mymap %></td>
      </tr>
    <% end %>
  </tbody>
</table>
        <content>
            <div class="prof">
                <% if @user.picture.url %>
                  <%= image_tag @user.picture.url, class: "prof-icon" %>
                <% else %>
                  <%= image_tag "no-picture.png", class: "prof-icon" %>
                <% end %>
                <div class="prof-name"><%= link_to @user.name, profile_user_path(@user) %></div>
            </div>
            <div class="follow-btn">
                <%= render 'relationships/follow_button', user: @user %>
            </div>
            <div class="discription">
                <p class="disc-1"><a href="/users/<%= @user.id %>/self_mymaps">お気に入り<br>マイマップ</a></p>
                <p class="disc-2"><a href="/users/<%= @user.id %>/like_mymaps">作成した<br>マイマップ</a></p>
            </div>

            <div class="boxContainer">
                <div class="box">
                    <p class="title">すべてのマイマップ</p>
                    <div class="dot-box">
                      <% @all_types.each do |type| %>
                        <% if type == 0 %>
                          <div class="dot" id="eat"></div>
                        <% elsif type == 1 %>
                          <div class="dot" id="see"></div>
                        <% elsif type == 2 %>
                          <div class="dot" id="buy"></div>
                        <% elsif type == 3 %>
                          <div class="dot" id="stay"></div>
                        <% end %>
                      <% end %>
                    </div>
                    <div class="hrt-box"></div>
                    <div class="name-box">
                        <p>someone's name</p>
                    </div>
                </div>
                <div class="box">
                    <p class="title">B級スポットマップ</p>
                    <div class="dot-box">
                        <div class="dot" id="eat"></div>
                        <div class="dot" id="see"></div>
                        <div class="dot" id="buy"></div>
                        <div class="dot" id="stay"></div>
                    </div>
                    <div class="hrt-box"></div>
                    <div class="name-box">
                        <p>someone's name</p>
                    </div>
                </div>
            </div>
        </content>
        <div class="enter">マイマップからスポットを検索</div>

